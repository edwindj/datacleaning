## @knitr rules
# numerical rules
age >= 0
height > 0
age <= 150
age > yearsmarried

# categorical rules
status %in% c("married","single","widowed")
aged %in% c("child","adult","elderly")
if ( status == "married" ) aged %in% c("adult","elderly")

# mixed rules
if ( age > 0 ) height > 0
if ( status %in% "married" ) age >= 17

